# Frontend Environment Variables
# For Next.js - these are PUBLIC and exposed to the browser

# WebSocket URL for backend connection
# In Docker deployment, this should be relative (same host)
# Format: ws://hostname:port/ws or wss://hostname:port/ws
NEXT_PUBLIC_WS_URL=ws://localhost:7860/ws
